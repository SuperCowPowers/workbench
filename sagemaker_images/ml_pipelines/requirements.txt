# Deps for the headless pipeline runner. Touches the full workbench surface
# (orchestration + modeling + analysis). The Dockerfile pre-installs torch
# from the PyTorch CPU index URL before processing this file, to claim the
# torch slot before sagemaker-serve's transitive resolution would otherwise
# grab the multi-GB CUDA wheel from pypi.
# Names only — versions are pinned by repo-root constraints.txt.
boto3
botocore
awswrangler
aiobotocore
sagemaker
redis
cryptography
requests
pandas
scikit-learn
xgboost
joblib
networkx
cleanlab
datasets
umap-learn
rdkit
mordredcommunity
ipython
pyyaml
torch

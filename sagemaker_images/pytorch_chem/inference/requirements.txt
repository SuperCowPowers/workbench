# Deps for the chemprop inference image. The Dockerfile passes the PyTorch
# cu130 index URL to pip; torch + chemprop come from there, everything else
# falls back to pypi via --extra-index-url.
# Names only — versions are pinned by repo-root constraints.txt.
boto3
botocore
awswrangler
requests
pandas
scikit-learn
joblib
rdkit
pyyaml
fastapi
uvicorn
torch
chemprop
